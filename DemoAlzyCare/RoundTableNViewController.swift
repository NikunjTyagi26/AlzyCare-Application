//
//  RoundTableNViewController.swift
//  DemoAlzyCare
//
//  Created by Nikunj Tyagi on 08/06/24.
//

import UIKit
import SwiftUI

class RoundTableNViewController: UIViewController {

    @IBOutlet var storyBoardView: UIView!
    override func viewDidLoad() {
        super.viewDidLoad()
//        let contentView = ContentViewMemory()
//
//        let hostingController = UIHostingController(rootView: contentView)
//                
//        // Add the hostingController as a child view controller
//        addChild(hostingController)
//        hostingController.view.translatesAutoresizingMaskIntoConstraints = false
//        storyBoardView.addSubview(hostingController.view)
//
//        // Add constraints to fill the parent view
//        NSLayoutConstraint.activate([
//            hostingController.view.topAnchor.constraint(equalTo: storyBoardView.topAnchor),
//            hostingController.view.leadingAnchor.constraint(equalTo: storyBoardView.leadingAnchor),
//            hostingController.view.trailingAnchor.constraint(equalTo: storyBoardView.trailingAnchor),
//            hostingController.view.bottomAnchor.constraint(equalTo: storyBoardView.bottomAnchor)
//        ])
//
//        // Notify the hostingController
//        hostingController.didMove(toParent: self)
//        // Do any additional setup after loading the view.
//        // Do any additional setup after loading the view.
    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
